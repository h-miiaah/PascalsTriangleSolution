/**
 * @param {number} numRows
 * @return {number[][]}
 */
 var generate = function(numRows) {
    var i = 0;
    var j = 0;
    var result = [];

    for (i = 0; i < numRows; i++) {
        result.push(Array(i + 1));
      }
  };